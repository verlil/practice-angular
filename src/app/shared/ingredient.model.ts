export class Ingredient {
  constructor(public name: string, public amount: number) {
    //this will create variables 'name' and 'ammount' and assign them values
    //that you send using a constructor
  }
}
